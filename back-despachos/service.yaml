apiVersion: v1
kind: Service
metadata:
  name: back-despachos-service
spec:
  selector:
    app: back-despachos
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: LoadBalancer
